---
description: Evaluate whether an idea is best deployed as a skill, slash command, plugin, subagent, or agent workspace
---

Use the `format-evaluator` skill to assess the following idea and recommend the best packaging format.

Read `docs/format-rubric.md` for the full rubric, then produce the structured verdict + recommendation output defined in `skills/format-evaluator/SKILL.md`.

Idea: $ARGUMENTS
